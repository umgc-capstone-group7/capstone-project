{% extends "base.html" %}
{% block content %}
  <h2 class="title">Sign In</h2>
  <p class="subtitle">Access your account center to plan classes, check GPA, and connect with support.</p>
  <form method="post" class="form-grid">
    {{ form.hidden_tag() }}
    <label>Email</label>
    {{ form.email(class="input") }}
    <label>Password</label>
    {{ form.password(class="input") }}
    <div class="actions">
      {{ form.submit(class="btn") }}
      <a class="link" href="{{ url_for('main.register') }}">No account yet? Create one</a>
    </div>
  </form>
{% endblock %}